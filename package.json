{
  "name": "blob-bunny",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "bun build index.ts --outdir=./dist --target=bun",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
